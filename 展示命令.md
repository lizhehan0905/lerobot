进入lerobot虚拟环境：  conda activate lerobot
进入工作目录： cd ~/VLA/lerobot/src/
查询can口号： bash piper_sdk/find_all_can_port.sh 
根据can 口号修改： /home/hpc/VLA/lerobot/src/activate_single_arm_can.sh  里的can口号
激活can口： bash activate_single_arm_can.sh
查询摄像头index号： PYTHONPATH=. python lerobot/find_cameras.py opencv    确认top和wrist视角的index，然后在对应的命令里修改index

一，关节作为action
（1）ACT模型

# 单包裹抓取
模型路径： /home/hpc/VLA/lerobot_models/act/merge_342_joint/pretrained_model    
描述：训练数据集包含302组，chenjun一人录制的正常抓取单包裹的数据，和40组桌面没有包裹的数据。
命令：
cd /home/hpc/VLA/lerobot/src/lerobot/scripts && python -m lerobot.inference_smooth --robot.type=single_piper --robot.id=single_piper_robot --robot.port=can1 --robot.cameras="{wrist: {type: opencv, index_or_path: 10, width: 640, height: 480, fps: 30},top: {type: opencv, index_or_path: 4, width: 640, height: 480, fps: 30}}" --policy.type=act --ckpt_path=/home/hpc/VLA/lerobot_models/act/merge_342_joint/pretrained_model  --task="You are a parcel sorter. Please pick up the packages and place them on the sorting table with the shipping labels facing up."

# 单包裹和多包裹抓取： 存在空抓的情况，其他情况能够正常抓取
训练模型：/home/hpc/VLA/lerobot_models/act/merge_data_1_8_922_train_1_9

效果：抽样测试结果，300000次的训练结果还行
命令：
cd /home/hpc/VLA/lerobot/src/lerobot/scripts && python -m lerobot.inference_smooth --robot.type=single_piper --robot.id=single_piper_robot --robot.port=can1 --robot.cameras="{wrist: {type: opencv, index_or_path: 10, width: 640, height: 480, fps: 30},top: {type: opencv, index_or_path: 4, width: 640, height: 480, fps: 30}}" --policy.type=act --ckpt_path=/home/hpc/VLA/lerobot_models/act/merge_data_1_8_922_train_1_9/checkpoints/300000/pretrained_model  --task="You are a parcel sorter. Please pick up the packages and place them on the sorting table with the shipping labels facing up."

# 单包裹和多包裹抓取：    推荐act展示该模型

效果：单包裹抓取蛮好，多包裹差一点
命令：/home/hpc/VLA/lerobot_models/act/act_training_1062_joint/checkpoints/100000/pretrained_model
cd /home/hpc/VLA/lerobot/src/lerobot/scripts && python -m lerobot.inference_smooth --robot.type=single_piper --robot.id=single_piper_robot --robot.port=can1 --robot.cameras="{wrist: {type: opencv, index_or_path: 10, width: 640, height: 480, fps: 30},top: {type: opencv, index_or_path: 4, width: 640, height: 480, fps: 30}}" --policy.type=act --ckpt_path=/home/hpc/VLA/lerobot_models/act/act_training_1062_joint/checkpoints/100000/pretrained_model  --task="You are a parcel sorter. Please pick up the packages and place them on the sorting table with the shipping labels facing up."


# 单包裹抓取效果勉强可以
训练模型：/home/hpc/VLA/lerobot_models/act/chenjun_342_joint_0_loss/01830000/pretrained_model
命令：
python -m lerobot.inference_smooth --robot.type=single_piper --robot.id=single_piper_robot --robot.port=can1 --robot.cameras="{wrist: {type: opencv, index_or_path: 10, width: 640, height: 480, fps: 30},top: {type: opencv, index_or_path: 4, width: 640, height: 480, fps: 30}}" --policy.type=act --ckpt_path=/home/hpc/VLA/lerobot_models/act/chenjun_342_joint_0_loss/01830000/pretrained_model  --task="You are a parcel sorter. Please pick up the packages and place them on the sorting table with the shipping labels facing up."

# 包裹翻转demo  
效果：实现单个指定包裹翻转，托盘位置固定
训练模型：/home/hpc/VLA/lerobot_models/act/flip_50/checkpoints/600000/pretrained_model
命令：
python -m lerobot.inference_smooth --robot.type=single_piper --robot.id=single_piper_robot --robot.port=can1 --robot.cameras="{wrist: {type: opencv, index_or_path: 10, width: 640, height: 480, fps: 30},top: {type: opencv, index_or_path: 4, width: 640, height: 480, fps: 30}}" --policy.type=act --ckpt_path=/home/hpc/VLA/lerobot_models/act/flip_50/checkpoints/600000/pretrained_model  --task="You are a parcel sorter. Plea
se pick up the packages and place them on the sorting table with the shipping labels facing up."


# pi05
训练模型：/home/hpc/VLA/lerobot_models/pi05/1_18_data1062/090000/pretrained_model

单多包裹 大概50%成功率
命令:
python -m lerobot.inference_smooth --robot.type=single_piper --robot.id=single_piper_robot --robot.port=can1 --robot.cameras="{wrist: {type: opencv, index_or_path: 4, width: 640, height: 480, fps: 30},top: {type: opencv, index_or_path: 10, width: 640, height: 480, fps: 30}}" --policy.compile_model=false   --policy.type=pi05 --ckpt_path=/home/hpc/VLA/lerobot_models/pi05/1_18_data1062/090000/pretrained_model  --task="You are a parcel sorter. Please pick up the packages and place them on the sorting table with the shipping labels facing up."



# 末端位姿

act 50组数据，20000轮训练结果 这个会出现偶尔扭曲现象，不建议使用

act 50组数据，250000轮训练结果，测试20组，未出现扭曲

cd /home/hpc/VLA/lerobot/src/lerobot/scripts && python -m lerobot.inference_new_eef --robot.type=single_piper --robot.id=single_piper_robot --robot.port=can1 --robot.ctrl_mode="eef" --robot.cameras="{wrist: {type: opencv, index_or_path: 12, width: 640, height: 480, fps: 30},top: {type: opencv, index_or_path: 4, width: 640, height: 480, fps: 30}}" --policy.type=act --ckpt_path=/home/hpc/VLA/lerobot_models/act/act_training_50_rotate6d_250000 --task="Pick up the express package and put it on the tray."


